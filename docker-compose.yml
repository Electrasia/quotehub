version: '3.8'

services:
  quodb:
    build: .
    ports:
      - "8000:8000"
    volumes:
      - ./config.json:/app/config.json
      - quodb_data:/app/data
    restart: unless-stopped

volumes:
  quodb_data:
