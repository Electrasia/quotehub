[pytest]
# Async test functions must be explicitly marked with @pytest.mark.asyncio.
# This is compatible with all pytest-asyncio versions (the old 'asyncio_mode'
# setting from v0.21+ is not used here).
